# Core runtime (minimal initial set)
fastapi==0.111.0
uvicorn==0.30.1
prometheus-client==0.20.0
dataclasses-json==0.6.4
pytest==8.2.2
pytest-cov==5.0.0
jinja2==3.1.4
psutil==5.9.8
httpx==0.27.0
aiohttp==3.9.5
pynacl==1.5.0
hypothesis==6.103.2
# HTTP client for integration tests / attack pattern script
requests==2.32.4
# Anomaly detection (Phase 4) - Isolation Forest
scikit-learn==1.4.2
# Phase 3 (SNN) optional neuromorphic dependencies. These are heavy; install only when enabling SNN.
torch==2.3.0
snntorch==0.8.0
# Lightweight seq2seq / instruction models (generation layer)
transformers==4.42.0

# Phase A RAG (optional re-ranking cross encoder)
sentence-transformers==3.0.1
jsonschema==4.23.0
PyYAML==6.0.2

# Security / IAM helpers (future)
# cryptography==42.0.8

# Observability (future)
# prometheus-client==0.20.0

# Dev / Quality (add to optional extras)
# ruff
# bandit
# detect-secrets
