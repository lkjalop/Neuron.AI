# Minimal dependency set for running a focused subset of tests (metrics + scanner + ingest)
# Excludes heavy ML/NLP packages that cause Windows long path issues.

pytest>=8.0.0
prometheus-client>=0.16.0
fastapi>=0.110.0
uvicorn>=0.29.0
pydantic>=1.10.13
orjson>=3.9.0
# If tests import scanner components that rely on aiohttp/httpx
httpx>=0.24.0
# Add any lightweight extras encountered during import failures.
