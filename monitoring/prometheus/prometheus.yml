global:
  scrape_interval: 15s
  evaluation_interval: 30s

rule_files:
  - rules.yml

scrape_configs:
  - job_name: 'neuron'
    metrics_path: /metrics
    static_configs:
      - targets: ['app:8000']
